{
  "name": "@happycatlabs/codex-client",
  "version": "0.2.3",
  "description": "Codex API client for orcastrator",
  "license": "MIT",
  "author": "HappyCat Labs",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/happycatlabs/codex-client.git"
  },
  "files": [
    "dist"
  ],
  "type": "module",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "lint": "oxlint src/",
    "format": "oxfmt .",
    "format:check": "oxfmt --check .",
    "prepare": "bun run build",
    "test": "bun test",
    "validate": "bun run format:check && bun run lint && bun run build && bun test"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "oxfmt": "^0.49.0",
    "oxlint": "^1.64.0",
    "typescript": "^5"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
